{
  "$id": "https://github.com/samcmill/wassail/src/data/udef.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "wassail udev data source building block",
  "type": "object",
  "required": [ "data", "hostname", "name", "timestamp", "uid", "version" ],
  "properties": {
    "data": {
      "description": "Dictionary of udev devices and attribute values",
      "minProperties": 1,
      "type": "object"
    },
    "hostname": {
      "description": "Hostname of the system where the data source was invoked",
      "type": "string"
    },
    "name": {
      "description": "building block name",
      "type": "string"
    },
    "timestamp": {
      "description": "Timestamp corresponding to when the data source was invoked",
      "type": "number"
    },
    "uid": {
      "description": "User ID of the user who invoked the data source",
      "type": "number"
    },
    "version": {
      "description": "version",
      "type": "number"
    }
  }
}
