{
  "$id": "https://github.com/samcmill/wassail/src/data/uname.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "wassail uname data source building block",
  "type": "object",
  "required": [ "data", "hostname", "name", "timestamp", "uid", "version" ],
  "properties": {
    "data": {
      "type": "object",
      "properties": {
        "sysname": {
          "description": "Name of the operating system implementation",
          "type": "string"
        },
        "nodename": {
          "description": "Network name of this machine",
          "type": "string"
        },
        "release": {
          "description": "Release level of the operating system",
          "type": "string"
        },
        "version": {
          "description": "Version level of the operating system",
          "type": "string"
        },
        "machine": {
          "description": "Machine hardware platform",
          "type": "string"
        }
      }
    },
    "hostname": {
      "description": "Hostname of the system where the data source was invoked",
      "type": "string"
    },
    "name": {
      "description": "building block name",
      "type": "string"
    },
    "timestamp": {
      "description": "Timestamp corresponding to when the data source was invoked",
      "type": "number"
    },
    "uid": {
      "description": "User ID of the user who invoked the data source",
      "type": "number"
    },
    "version": {
      "description": "version",
      "type": "number"
    }
  }
}
